// pdf.js — Generación de tickets PDF con jsPDF (Simulado)

export function generateClientTicket(invoice) {
  console.log('Generando ticket de cobro para el cliente:', invoice);
}

export function generateServiceTicket(invoice) {
  console.log('Generando ticket de servicios para el profesional:', invoice);
}
